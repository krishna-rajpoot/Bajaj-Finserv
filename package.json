{
  "name": "deskflow",
  "version": "1.0.0",
  "description": "api",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "joi": "^18.2.1"
  }
}
